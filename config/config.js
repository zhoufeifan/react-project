const version = "0.0.0";
const rootPath = __dirname.replace(/\/(\d|[a-z]|[A-Z])+$/,"");
const daliyDestUrl = `../assets/teddy`;
const preDestUrl = `../pre-publish-assets/teddy`;
module.exports = {
    version,
    rootPath,
    daliyDestUrl,
    preDestUrl,
};
